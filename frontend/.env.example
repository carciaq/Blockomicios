# URL base de la API del backend (nodo/gateway blockchain)
VITE_API_URL=http://localhost:3000/api

# Si es "true", los repositorios usan datos simulados en vez de llamar a la API real.
# Útil mientras el backend blockchain aún no está disponible.
VITE_USE_MOCKS=true
